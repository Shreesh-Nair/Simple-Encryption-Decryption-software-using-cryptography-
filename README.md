# Simple-Encryption-Decryption-software-using-cryptography-
-The encrypt program encrypts all the available files in the current directory using cryptography and generates a key file which contains the key
-The decrypt program decrypts all the available files in the current directory using the key generated by the input
-The above 2 programs can be modified by the user to fit their needs
